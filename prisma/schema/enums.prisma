enum activeStatus {
    ACTIVE
    BLOCKED
}

enum Role {
    USER
    AUTHOR
    ADMIN
}

enum PostStatus {
    DRAFT
    PUBLISHED
    ARCHIVED
}

enum CommentStatus {
    PENDING
    APPROVED
    REJECTED
}
